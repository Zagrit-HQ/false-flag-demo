---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: flagsnapshots.falseflag.dev
spec:
  group: falseflag.dev
  names:
    kind: FlagSnapshot
    listKind: FlagSnapshotList
    plural: flagsnapshots
    shortNames:
    - ffsnap
    singular: flagsnapshot
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.projectSlug
      name: Project
      type: string
    - jsonPath: .status.compiledVersion
      name: Version
      type: integer
    - jsonPath: .status.flagCount
      name: Flags
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Compiled")].status
      name: Compiled
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          FlagSnapshot mirrors the latest immutable compiled snapshot for a
          project. Read-only on the user side; written by the operator.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              FlagSnapshotSpec defines the desired state of a FlagSnapshot CR.
              FlagSnapshot is read-only: the operator polls the upstream API for
              the latest snapshot and writes the version into status.
            properties:
              projectSlug:
                description: ProjectSlug identifies the upstream project.
                pattern: ^[a-z][a-z0-9-]{0,62}$
                type: string
            required:
            - projectSlug
            type: object
          status:
            description: FlagSnapshotStatus is the observed state of a FlagSnapshot.
            properties:
              compiledVersion:
                description: |-
                  CompiledVersion is the snapshot version most recently returned
                  by GetLatestSnapshot. Zero when no snapshot exists yet.
                format: int32
                type: integer
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              flagCount:
                description: |-
                  FlagCount records how many flags were present in the latest
                  snapshot.
                format: int32
                type: integer
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
