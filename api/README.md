# API Contracts

API contract sources live here.

- `openapi/**` contains REST/OpenAPI definitions.
- `proto/**` is intentionally root-level at `proto/**`, following the Go repository reference shape.
