# Evaluation

Flag evaluation engine and normalized release snapshot logic.

Runtime SDKs and the proxy should evaluate static JSON/rules from compiled snapshots. They should not execute user-authored TypeScript.
