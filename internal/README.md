# Internal Go Packages

All Go implementation code belongs under `internal/**`.

Do not create a Go `pkg/**` tree. This repository intentionally follows the shape of `/Users/wito/code/project-depot/registry`: binaries in `cmd/**`, implementation packages in `internal/**`, SQLC output in `internal/db`, and contracts at the root.
