{
  "id": "always-match",
  "description": "The 'always' predicate unconditionally fires its rule.",
  "strategy": "json",
  "ir": {
    "value_type": "string",
    "default": "fallback",
    "rules": [
      {
        "id": "always-on",
        "when": { "kind": "always" },
        "value": "everyone"
      }
    ]
  },
  "context": {},
  "expected": { "value": "everyone", "reason": "rule_matched", "rule_id": "always-on" }
}
