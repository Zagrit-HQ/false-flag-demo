# Config TypeScript DSL

TypeScript config-as-code DSL package.

This package should expose the pure authoring API used by TypeScript configuration files. Server-side compilation must produce static release snapshots; runtime SDKs and proxy services must not execute user-authored TypeScript.
