# TypeScript Workspace

All TypeScript code and TypeScript workspace files belong under `js/**`.

Expected tooling:

- pnpm workspaces
- Turborepo
- Biome
- Vitest
- Orval
- Playwright

Do not place TypeScript apps or packages at the repository root.
