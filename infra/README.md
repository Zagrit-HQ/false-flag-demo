# Infrastructure And Build Graph

Build and local infrastructure configuration.

Expected files include Dockerfiles and CI-supporting scripts. The Docker Bake build graph lives at the repository root as `docker-bake.hcl`.
